# PR18 机载验证修改简报

对比基线：PR18 提交 `89a1b1525f284db124f130a9978d85a16f083c39`。

本次只整理在 NUC + Livox MID360S 真机上验证过的统一后端改动，不修改
FAST-LIO、建图逻辑、飞控、GNSS、视觉、光流或 MID360S 网络配置。

## 修改背景

PR18 已经包含 MID360S 加速度从 `g` 到 SI 单位的换算，静止时后端不再出现
数十万米量级发散。但真机测试中，`/fast_lio/native_lidar_factor` 稳定在约
9 Hz，而 `/fusion/unified/odom` 通常只有 1--2 Hz。诊断数据表明输出链存在
两处竞争：

- IMU 前向传播计算期间，原生激光优化可能提交一个新锚点，使正在计算的旧锚点
  结果因 `anchor_changed` 被丢弃；
- 优化状态提交时，定时传播可能已经发布了时间戳更新的里程计。优化里程计会被
  时间戳单调性保护正确抑制，但新优化锚点没有立即产生对应的前向输出。

## 相对 PR18 的代码差异

### `uf_backend_fusion/online_backend.py`

- 将代码内执行器线程默认值从 3 调整为 4。PR18 的 YAML 已配置为 4，因此正常
  使用配置文件启动时参数值不变。
- 当 IMU 预积分过程中优化锚点发生变化时，使用新锚点最多重试一次传播发布。
- 当优化里程计仅因已有更新输出时间戳而被抑制时，立即基于刚提交的新锚点执行
  一次“只发布、不修改因子图”的前向传播。
- 新锚点同步传播只绕过 `lidar_recent` 节流；IMU 新鲜度、状态 epoch、扫描预测
  契约、最小发布间隔和时间戳单调性保护仍然有效。
- 将关键输出链计数器移动到诊断数组前部，避免长诊断消息经 DDS 或 CLI 截断后
  看不到关键字段。

### `uf_backend_fusion/live_propagation.py`

- 新增可选参数 `allow_recent_lidar=False`。
- 默认值保持 PR18 原有行为；只有在优化锚点刚提交后的同步传播中才设为 `True`。

### 测试代码

- 验证普通传播仍会因近期激光活动而被拒绝。
- 验证只有显式的新锚点模式能够绕过该节流。
- 更新锚点并发测试，要求锚点变化后从新锚点成功重试一次。

## 真机测试结果

测试组合为 MID360S 点云 + MID360S IMU + FAST-LIO + 统一后端；本轮地面测试
不需要 GNSS、视觉、光流或 MAVROS。

- 修改前：`/fusion/unified/odom` 通常约 1--2 Hz，并出现大量
  `anchor_changed` 拒绝和优化输出非单调抑制。
- 修改后：60 秒测试最终稳定在约 4.8 Hz，高于 3 Hz 目标。观测到的最大间隔
  约 1.25 秒，因此试飞前仍需继续进行延迟与长时间地面测试。

## 安全状态

以上仅属于地面验证，不代表已经具备飞行资质。在轨迹方向、返回原点误差、静止
漂移、时间戳连续性和失效保护测试全部通过前，不要将
`/fusion/unified/odom` 接入飞控外部导航。
