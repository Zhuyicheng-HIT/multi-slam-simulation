# 当前阶段报告：仿真轻量化与多源融合基础生成时间：2026-07-21 11:08:39 +08:00仓库：`$HOME/multi-slam-github-staging`分支：`feature/ultra-fusion-stage3`最近已提交基线：`fd8caf5 feat: validate stage 4 aiding sensor foundations`## 1. 停止状态本次暂停前已检查运行环境，没有残留 Gazebo、ArduPilot SITL、MAVROS、FAST-LIO、ROS 2 bag 或项目节点。当前没有继续编译、启动仿真或执行航线；工作区保留未提交的迭代改动，未创建新提交，也未推送远程仓库。## 2. 已完成并有证据的内容### 2.1 仿真运行轻量化- D435i 派生点云默认关闭，但保留彩色、深度和 IMU 原始接口。- 使用传感器 QoS 的 `BEST_EFFORT + KEEP_LAST(1)`，优先处理最新帧，避免旧帧堆积。- 光流、BDS/GNSS 和 ExternalNav 链路保留在机载电脑侧；飞控只接收最终 ExternalNav。- 启动脚本清理逻辑已加强，正常停止和异常退出后不会继续占用仿真资源。- 轻量配置实测仿真实时因子约为 `0.9998`，说明当前主要开销不是仿真时间推进，而是传感器和算法进程的派生计算。### 2.2 光流和 BDS 辅助定位当前架构是：```text光流原始观测 ─┐              ├─> 机载电脑 GPS/Flow fusion ─> /mavros/odometry/out ─> ArduPilot ExternalNavBDS/GNSS 原始观测 ─┘```- 已关闭把光流直接作为飞控主定位输入的旧路径，避免与 ExternalNav 双重融合。- 光流评估器已改用消息内 `integration_time_us` 作为积分区间；到达间隔只作为调度和诊断指标。- 固定矩形航线重复运行 3 次，均完成航线、转弯和降落，且 `ekf_using_gps=False`。| 试验 | ATE (m) | RPE-1 (m) | RTF | 光流 Hz | GNSS Hz | ExternalNav Hz | 光流相关 | 光流 NRMSE ||---|---:|---:|---:|---:|---:|---:|---:|---:|| 1 | 0.1140 | 0.1012 | 0.9999 | 15.00 | 9.64 | 20.00 | 0.973 | 0.363 || 2 | 0.1256 | 0.1111 | 0.9998 | 15.00 | 9.56 | 20.00 | 0.974 | 0.363 || 3 | 0.0923 | 0.0873 | 0.9999 | 13.91 | 9.72 | 18.69 | 0.974 | 0.294 || 平均 | 0.1106 | 0.0999 | 0.9999 | 14.64 | 9.64 | 19.56 | 0.974 | 0.340 |详细数据在 `externalnav_lightweight_trials.csv` 和 `optical_flow_report.md`。### 2.3 ReliabilityScheduler已实现并通过当前 ROS 2 单元/集成测试：- `D -> s=1-D` 的连续权重；- 协方差膨胀；- 因子二值关闭和恢复滞回；- `NORMAL / DEGRADED / RISK / RELOCALIZING / RECOVERED / FAILSAFE` 状态；- 新鲜度超时、非法分数和恢复保持时间；- `/reliability/scheduler_state` 和 `/relocalization/request` 接口。Scheduler 核心、节点测试和现有传感器测试合计通过 42 项。当前仍未把含真实 LIO 退化分数的 Scheduler 接入一次完整飞行，因此“代码通过”不等于“在线融合验收通过”。## 3. 本次暂停时新增、但尚未验证的改动刚加入了项目侧 LiDAR 时序体素过滤：- `TemporalVoxelFilter`：首次出现的体素标为不确定，连续支持后进入静态集合，成熟窗口中无支持的体素判为动态；- 新输出 `/lidar/static_cloud`、`/lidar/dynamic_cloud`、`/lidar/uncertain_cloud`；- `LioDiagnostics` 增加动态比例、不确定比例、特征重复率和三类点数；- `/lio/local_map` 只追加静态点，避免动态点和不确定点污染项目侧局部地图；- 默认时序窗口 5 帧、静态支持 2 帧、邻域半径 1 个体素。这些文件已经写入工作区，但尚未重新编译、运行测试或经过 Gazebo 航线验证，不能作为当前验收结果。该过滤器只保护项目侧 `/lio/local_map`，不等于已经修改 FAST-LIO 内部地图；若要在 FAST-LIO 配准前剔除动态点，后续需要在其点云输入或内部地图更新处接入。## 4. 阶段完成度| 阶段 | 状态 | 说明 ||---|---|---|| 0 仓库与环境审计 | 基本完成 | 审计、依赖计划、ROS 2 workspace 和论文公式记录已存在。 || 1 仿真传感器数据层 | 基本完成 | 话题、时间戳、QoS、故障注入配置已具备。 || 2 LiDAR-IMU 前端基线 | 部分完成 | 现有 FAST-LIO 链路和 LIO 诊断适配器存在；新的动态保护尚未验收。 || 3 可靠性评分 | 基本完成 | L/BDS/IMU/光流/RGB-D 评分代码和验证报告已存在，需继续用真实故障注入校准。 || 4 BDS/光流辅助定位 | 基础验收完成 | 3 次固定航线通过；仍需 BDS 跳变、中断和恢复回放。 || 5 动态剔除与地图保护 | 实现中 | 本次新增代码待编译、单测和航线验证。 || 6 ReliabilityScheduler | 实现完成、在线未验收 | 核心和 ROS 集成测试通过；等待 LIO 动态分数接入完整回放。 || 7 统一滑窗后端 | 未开始 | 目前不提前硬写完整统一滑窗。 || 8 重定位与闭环评测 | 未开始 | 等稳定观测和调度器在线验证后开始。 |## 5. 仍然存在的工程缺口1. 当前 MAVROS `nav_msgs/Odometry -> MAVLink ODOMETRY` 路径没有完整暴露 MAVLink `quality` 和 `reset_counter` 语义；ExternalNav 基础通道可用，但重定位复位计数和质量字段仍需专用桥或 MAVROS 扩展。2. 光流和 GNSS 已在机载电脑侧融合，但还没有把来自 LIO 的独立退化分数作为 Scheduler 的在线输入。3. LIO 适配器的点到面指标仍是外部近似诊断，不是 FAST-LIO 内部残差、Hessian 或地图质量的原生导出。4. 尚未做 BDS 中断/跳变、动态目标、低纹理光流以及并发退化的自动回放矩阵。5. 当前工作区改动仍未按关键版本提交；下一次验证通过后再形成一个可回滚提交，避免把未验证的动态过滤器和已验收的 ExternalNav 混在同一个里程碑中。## 6. 恢复后的最小执行顺序1. 编译 `uf_interfaces`、`uf_lio_adapter` 和 `uf_reliability`，运行几何过滤器和全部相关测试。2. 启动原有测试地图的轻量仿真，确认三个 LiDAR 分类话题、`/lio/diagnostics` 和 `/lio/local_map` 的消息频率、点数和时间戳。3. 运行同一固定矩形航线，比较启用/关闭时序过滤器时的 ATE、RPE、动态比例、重复率和局部地图点数。4. 只有上述结果稳定后，才把 LIO 分数接入 ReliabilityScheduler 的在线 launch，并做一次 NORMAL -> DEGRADED/RISK -> RECOVERED 回放。5. 最后再提交一个关键版本并推送；若指标变差，回退动态过滤器参数或代码，不影响已验收的 ExternalNav 基线。