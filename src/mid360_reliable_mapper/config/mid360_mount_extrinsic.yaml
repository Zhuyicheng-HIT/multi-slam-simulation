# MID360S mount TF wrapper. Numeric frames and transforms are intentionally
# owned by the canonical hardware geometry contract below, rather than being
# duplicated here. FAST-LIO's internal LiDAR-to-IMU extrinsic is independent.

enabled: true
geometry_contract_file: package://uf_sensor_pipeline/config/real_mid360s_d435i_geometry.yaml
publish_inverse_for_fastlio: false
